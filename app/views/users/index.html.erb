<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-xs-3">
      <%= render 'users/profile' %>
      <div class="mt-3">
        <%= render 'books/form', book: @book %>
      </div>
    </div>
    <div class='col-xs-9 offset-md-1'>
      <%= render 'users/list', books: @users %>
    </div>
  </div>
</div>